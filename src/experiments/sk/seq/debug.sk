bit[8] rnd_bits;

int next_rnd(){
    bit new_bit = rnd_bits[0] ^ rnd_bits[1];
    rnd_bits[0::7] = rnd_bits[1::7];
    rnd_bits[7] = new_bit;
    return (int)rnd_bits;
}

harness void main(){
    assert next_rnd() < 256;
    
}
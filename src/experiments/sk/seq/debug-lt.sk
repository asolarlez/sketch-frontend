// Debug sketch file, to be modified by Liviu only

int S=2;
int P=S+S;

bit foo(bit[P] in){
   return in[{*}];
}

bit revsk(bit[P] in) implements reverse {           
   return foo(in);
}

bit reverse (bit[P] in) {
   return in[S];
}

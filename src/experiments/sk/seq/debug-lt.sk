// Debug sketch file, to be modified by Liviu only

bit foo(bit[4] in){
   return in[{*}];
}

bit revsk(bit[4] in) implements reverse {           
   return foo(in);
}

bit reverse (bit[4] in) {
   return in[2];
}

pragma options "--synth ABC --cbits 7	 --showphase final --keepasserts --unrollamnt 31 --verbosity 1";

int dummy(int x) { return 1; }

#define N 30

int[2*N] _r_; 
int _i_;
int _Choice_() { int t = _i_; _i_++; return _r_[t]; }
#define _ (_Choice_())


int harness (int x) implements dummy {
	_r_ = ??; 
	_i_ = 0;

	// generate inputs
	int i = 0;
	for (i = 114; i < 137; i++) {
		int t1 = _; int t2 = _;
		assert t1 > 1 && t2 > 1;
		if (i != 127 && i != 131) 
			assert t2 * t1 == i;
	}
	return 1;
}


void main(int x) {
	assert x == 1 * _;  // x+1 >= t && t > x;
	return; 
}

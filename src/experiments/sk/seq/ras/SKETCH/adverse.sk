pragma options "--debug-cex --slv-synth ABC  --bnd-inbits 12 --bnd-cbits 12 --debug-show-phase final --fe-keep-asserts --debug-verbosity 1";

void main(int x) implements one{
	int r;
	
	r = x-500;
	if (x < ??)
		r = -r;
	assert (r>=0); 
}

void one(int dummy) {
	
}

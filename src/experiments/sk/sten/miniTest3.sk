int N;
int[N] test(int[N] in){
  int[N] out;
  for(int i=0; i<N; ++i){
  	if(i>0 && i<N) out[i] = in[i-1] + in[i+1];
  	else out[i] = 0;
  }
}

int[N] testSK(int[N] in) implements test {
  int[N] out = 0;
  for(int i=??; i<N-??; ++i){  	
  	out[i+1] = in[i];
  }
  for(int j=??; j<N-??; ++j){  
    	out[j-1] += in[j];
  }
}
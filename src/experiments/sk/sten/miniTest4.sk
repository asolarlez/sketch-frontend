int N;
int[N] test(int[N] in){
  int[N] out;
  for(int i=0; i<N; ++i){
  	if(i>0 && i<N) out[i] = in[i-1] + in[i+1];
  	else out[i] = 0;
  }
  return out;
}

int[N] testSK(int[N] in) implements test {
 int[N] out;
  for(int i=??; i<??; ++i){
    out[i] = 0;
  	for(int j=0; j<??; ++j){
  		if(??){
		  	out[i] += in[i+??];
	  	}else{
		  	out[i] += in[i-??];
	  	}
	}
  }
  return out;
}


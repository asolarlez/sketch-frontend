bit[2] reverseSketch(bit[4] in) implements reverse {	
	bit[2] t;
	t = (bit[2]) in;
	return t;
}

bit[2] reverse (bit[4] in) {
	bit [2] tmp=0;
	for(int i=0; i<2; ++i){
		tmp[i] = in[i];
	}
	return tmp;
}
bit[6] reverseSketch(bit[4] in) implements reverse {	
	bit[6] t;
	t = (bit[6]) in;
	return t;
}

bit[6] reverse (bit[4] in) {
	bit [6] tmp=0;
	for(int i=0; i<4; ++i){
		tmp[i] = in[i];
	}
	return tmp;
}
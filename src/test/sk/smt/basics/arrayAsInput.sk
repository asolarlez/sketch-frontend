pragma options "--bnd-intbits 32 --bnd-cbits 32 --bnd-inbits 7";

void main(int[2] dummy) implements one{
	
	
	if (dummy[0] == 30) {
		assert dummy[0] + ?? == 130; // ?? == 100
	}
	
	if (dummy[1] == 40) {
		assert dummy[1] + ?? == 240; // ?? == 200
	}
}
void one(int[2] dummy) {}
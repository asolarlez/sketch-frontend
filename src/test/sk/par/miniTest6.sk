

struct Box {
	int val;	
}

Box newBox(int i){
	Box b = new Box();
	b.val = i;
	return b;	
}

int N = 2;

bit test(int in){	
	return 1;
}

bit testSK(int in) implements test{
	Box global = newBox(in);
	ploop(int i; 2){
		int local;
		lock(1);
		local = global.val;
		local = local + 1;
		global.val = local;
		unlock(1);		
	}
	return global.val == in + 2;
}




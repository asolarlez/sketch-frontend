

int N = 2;

bit test(int in){	
	return 1;
}

bit testSK(int in) implements test{
	int global = in;
	ploop(int i; 2){
		int local;
		anyorder{	
			local = global + 1;
			global = local;	
		}
	}
	return global == in + 1 || global == in + 2;
}




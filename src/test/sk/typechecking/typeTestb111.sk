// Local variables do not pick formal parameters because they 
// do not have a type.

harness void main(){	
	bit x = 1;
		
    fun f = (x) -> $(int) + 2; // TypeError possible capture.
                  
    assert f(10) == 12;
}

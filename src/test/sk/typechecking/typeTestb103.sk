// Testing shadowing of function

harness void main(){	
    fun f = (t) -> t + 1;

    int x = f(3);
    int y = f(2);
    
    int x = 3;
    
  	// f = (t) -> t + 5; // Error since f was already defined.
  	
  	fun f = (t) -> t + 5;//Should not work, but it is!
    
    
    assert x + y == 7;
}



// Passing the wrong number of parameters in the lambda expression

int five(fun f, int x, int y) {	
	return f(x, y);
}

harness void main(){	
	int four = 4;
	int one = 1;
	
    assert five((tfour) -> tfour + 5, four, one) == 5; //TypeError
}

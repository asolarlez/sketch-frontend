int N;
float[N][N] test0(float[N][N] a){
  float[N][N] out=0;
  for(int i=0; i<N; ++i){
  	for(int j=0; j<i; ++j){
  		out[i][j] = a[i][j];
  	}
  }
  return out;
}

float[N][N] testSK0(float[N][N] a) implements test0 {
  float[N][N] out=0;
  for(int i=0; i<N; ++i){
  	for(int j=0; j<i; ++j){
  		out[i][j] = a[i][j];
  	}
  }
  return out;
}
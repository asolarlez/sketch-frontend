    harness void main(int x) {
        int h = ?? + 1;
        int v = h * x;
        assert (v % 2) == 0;
        minimize(400 - h);
        // returns 2
        // minimize(h);

        assert h > 0;
    }

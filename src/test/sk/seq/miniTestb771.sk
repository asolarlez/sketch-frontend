// Casting an expression to a function that passes a local variable
// and a formal parameter

int apply(fun f, int y) {
	return f(y);
}

harness void main() {
	int joe = 3;
	
	assert apply(joe + x, 8) == 11;
}
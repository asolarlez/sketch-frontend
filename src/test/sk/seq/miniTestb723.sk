pragma options "-V 0";

bit reverseSketch(bit[4] in) implements reverse {	
	bit t=0;
	int i=0;
	int j = 0;
	repeat(??){
		t = in[$$$];
		i = $$$+??;
	}
	return t;
}


bit reverse (bit[4] in) {
	return in[2];
}

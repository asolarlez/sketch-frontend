package TT;

struct MultiType{
    MTArray lost;
    int val;
    int flag;
    int fofo;
}

int INTEGER=0;
int LIST=1;

struct MTArray{
int len;
MultiType[len] B;
}

MultiType foo(){
    return  new MultiType(val=5, flag=INTEGER, fofo=3);
}
harness void main1(int x) { /* automatically rewritten */
    assert(x - x + ?? == 3);
}

harness int main2(int x) { /* automatically rewritten */
    int v = ??;
    assert(v + 1 == 3 - 1);
    return v;
}

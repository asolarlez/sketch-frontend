// Testing that an lambda expression creates a specialized function by passing a local variable

int five(fun f, int x) {	
	return f(x);
}

harness void main(){	
	int four = 4;
	
    assert five((,x) -> x + 1, four) == 5;
}

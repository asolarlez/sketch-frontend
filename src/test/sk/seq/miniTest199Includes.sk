pragma options "--bnd-unroll-amnt 6 ";

#ifndef __SKETCH__
#error This file was only intended to be run with the SKETCH compiler
#endif

#include "miniTest199Includes.skh"

bit main_fun(bit a, bit b, bit c) implements VOID{
        return (a & c) & !b;
}

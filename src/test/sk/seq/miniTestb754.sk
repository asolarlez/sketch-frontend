adt tt {
	foo {tt a; tt b;}
	bar {tt v; tt a; tt b;}
	num {int x;}	
}

harness void main() {
	tt a = ??;
	bar b = (bar) a;
	num n1 = (num) b.v;
	num n2 = (num) b.a;
	assert(n1.x == 2);
	assert(n2.x == 3);	
}
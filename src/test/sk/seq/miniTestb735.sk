// Testing passing a local lambda to a function that expects a function

int apply(fun f, int x) {
	return f(x);
}

harness void main(){		
    fun f1 = (,y) -> y + 5;
              
    assert apply(f1, 5) == 10;
}

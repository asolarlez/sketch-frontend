bit[3] miniTest(bit[3] in) {
	bit[2] one = {0, 0};
	one[0] = 1;
    return (in<<1) ^ one;
}


bit[3] miniTestSK (bit[3] in) implements miniTest{
	bit[3] tmp = (in<<??);
	bit[2] one = {0, 0};
	repeat(??){
		one[0] = ??;
	}
    return tmp ^ one;
}

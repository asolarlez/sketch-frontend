
int spec(int x, int y){
	int t = x + y;
	t = t + 3;
	return t;
}

int sketch(int x, int y) implements spec{
	int g = ??;
	int h = ??;
	// assert h == 2 && g == 1;
	int d = 0;
	int f = (g==1) ? x + y : 0;
	int e = (g==1) ? 3 : 0;
	int tt = e + f;
	int aa = (g==2) ? tt : 0;
	int b = (g==2) ? aa + 1 : 0;	
	int a = (g==3) ? b : d;
	int x0 = (h==4) ? a : tt;
	
	return x0;
}

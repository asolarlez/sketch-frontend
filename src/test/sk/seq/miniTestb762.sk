// Making sure that each local variable expression has its own set
// of possible variables

harness void main() {
	int a = 1;
	int b = 2; 
	
	int x = $(int) + 10;
	
	bit c = 0;
	bit d = 1;

	bit y = $(bit);
	
	assert x == 12;
}
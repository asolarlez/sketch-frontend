bit reverseSketch(bit[4] in) implements reverse {	
	bit [5] tmp = {*};
	bit [2] tmp2 = {*};
	int lll = ((int)tmp2);
	int ttt = (int)(tmp>>lll);
	assert ttt < 4;
	return in[ttt];
}


bit reverse (bit[4] in) {
	return in[2];
}
bit reverseSketch(bit[3] in, bit[3] in2) implements reverse {
	int [8] fu=0;
	int i=0;
	repeat(8){
		fu[i] = ??;
		++i;
	}
		
	int idx1 = (int) in;
	int idx = fu[ idx1 ];
	return in2[idx];
}


bit reverse (bit[3] in, bit[3] in2) {
	int [8] fu = { 2, 1, 1, 0, 1, 2, 2, 0};
	int idx1 = (int) in;
	int idx = fu[ idx1 ];
	return in2[idx];
}


harness void main(){
    int x = 0;
    int foo(){
        x++;
        return x;
    }
    
    fun f = (x)-> x + x + x;
    
    //int f(int x){ return x + x + x; }
    
    //assert foo() == 1;

    assert f(foo()) == 3;
}
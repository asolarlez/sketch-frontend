// Testing that the expression of a lambda function can have another function

harness void main(){	
    fun f1 = (,x) -> x + 1;
    fun f2 = (,y) -> 1 + f1(y);
    fun f3 = (,z) -> f2(z) + 1;
    
    int temp = f3(2);
    
    assert temp == 5;
}

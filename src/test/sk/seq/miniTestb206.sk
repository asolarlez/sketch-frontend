bit[5] spec(bit[5] in){    
    in[1::2] = in[0::2];    
    return in;
}

bit[5] sk(bit[5] in) implements spec{
    bit[2] t = in[0::2];
    in[1::2] = t;
    int x = 3;
    int[3] fa = 0;
    fa = {0,1,2,3,4,5,6,7}[x::3];
    int tt = ??;
    assert fa[1] == tt;
    assert tt == 4;
        
    return in;
}
// 

int apply(fun f, int x) {
	return f(x);
}

int test() {
	fun f = (,x) -> x - 5;
	int a = apply(f, 10);
	
	return a;
}

harness void main(){		
    fun f = (,x) -> x + 5;
    
    int o = test();
              
    assert apply(f, 5) == 10;
}

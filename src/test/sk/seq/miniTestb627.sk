int foo([int n], int[n] arr){
	
	if(n>??){
		return foo(n-1, arr[0::n-??]);
			
	}else
	if(n>??){
		return foo(n-2, arr[0::n-??]);
		
	}else{
		return 1; 
	}
	
}
harness void sketch(){
	
	assert(foo({1,2,3})==1);	
}
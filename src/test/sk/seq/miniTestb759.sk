// Testing that lambdas consider formal parameters

int apply(fun f, int x) {
	return f(x);
}

harness void main(){		
    fun f = (,y) -> $(int) + 5;
                  
    assert apply(f, 5) == 10;
}

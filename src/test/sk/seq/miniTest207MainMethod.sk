pragma options "--main_names main,main2";

generator void main1(int x) { /* automatically rewritten */
    assert(x - x + ?? == 3);
}

generator void main2(int x) { /* automatically rewritten */
    assert(?? + 1 == 3 - 1);
}

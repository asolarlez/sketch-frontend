// Used to be a problem with operator precedences.

int spec (int a, int b, int c, int d) {
    return (a | (b ^ (c & d)));
}
int sk (int a, int b, int c, int d) implements spec {
    return a | b ^ c & d;
}

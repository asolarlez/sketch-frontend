// test of bounded variable length array

void f(int n, bit [n <: 2] a, bit[n <: 2][n <: 2] b) {
	a[n-??] = 1;
	a[0] = a[n-??];
	b[n-1][n-1] = a[??];
	assert(b[n-1][n-1] == 1);
}

harness void main(){
	bit[2] a = {0};
	bit[2][2] b = {0};
	f(2, a, b);
}
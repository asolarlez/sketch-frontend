bit miniTest(bit[4] in) {
	return in[!in[0]];
}


bit miniTestSK (bit[4] in) implements miniTest{
	bit x;
	if(in[0]){
		x = in[??] {|}  ??;
	}else{
		x = in[??] {|} ??;
	}
	assert x == !in[0];
    return in[x];
}

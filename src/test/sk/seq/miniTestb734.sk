// Testing passing a local variable inside a multiple lambda expressions

int apply(int x, fun f1, fun f2) {
	return f1(x) + f2(x);
}

harness void main(){	
    int local1 = 1;
    int local2 = 2;
              
    assert apply(2, (,x) -> x + local1, (,y) -> local2 + y) == 7;
  
}
